# .env.example
# This is a template for environment variables. Copy this file to '.env' and update the values.
# Never commit the actual .env file to git - it contains sensitive information!

# Server Configuration
PORT=3000                    # The port your server will run on
NODE_ENV=development        # 'development' enables debugging, 'production' optimizes for performance

# Database Configuration
DB_HOST=localhost          # Database location - use 'db' if running in Docker
DB_USER=tottp_user        # Database username
DB_PASSWORD=your_password_here    # Database password - change this!
DB_NAME=tottp_db          # Name of your database

# JWT (JSON Web Token) Configuration
JWT_SECRET=your_jwt_secret_here   # Secret key for signing JWTs - make this long and random!
JWT_EXPIRES_IN=24h               # How long tokens remain valid

# Security
BCRYPT_SALT_ROUNDS=10            # Higher numbers = more secure but slower password hashing

# Logging
LOG_LEVEL=debug                  # Options: error, warn, info, debug (most detailed)